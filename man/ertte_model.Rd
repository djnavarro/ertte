% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ertte-core.R
\name{ertte_model}
\alias{ertte_model}
\title{Fit an exposure-response time-to-event model based on \code{survreg()}}
\usage{
ertte_model(formula, data, dist = "weibull", ...)
}
\arguments{
\item{formula}{Model formula, with a \code{survival::Surv()} object as the
response, e.g. \code{Surv(time, event) ~ exposure}.}

\item{data}{Data set}

\item{dist}{The AFT distribution to fit, as for \code{survival::survreg()}.
Defaults to \code{"weibull"}. Tested and officially supported for
\code{"exponential"}, \code{"weibull"}, \code{"lognormal"}, and \code{"loglogistic"} --
see \code{\link[=ertte_select_distribution]{ertte_select_distribution()}} for choosing among them by AIC.}

\item{...}{Other arguments passed to \code{survival::survreg()}.}
}
\value{
A survreg object with an extra \code{ertte_model} class
}
\description{
Fit an exposure-response time-to-event model based on \code{survreg()}
}
\details{
The returned object has class \code{c("ertte_model", "survreg")}:
it \emph{is} a \code{survreg} object, with a little extra metadata attached.
This means all of the usual \code{survreg} methods work unchanged, without
needing an ertte-specific equivalent -- e.g. \code{summary()}, \code{coef()},
\code{vcov()}, \code{confint()}, \code{predict()}, \code{AIC()}, \code{BIC()}, \code{logLik()}, and
\code{anova()} for comparing nested models. \code{ertte_predict()} is a
separate, ertte-specific alternative to \code{predict()} that returns
survival probabilities with confidence intervals in a tidy data
frame; the two are complementary, not competing.

All four supported distributions are log-location-scale AFT models:
\code{log(T) = mu + scale * W}, where \code{mu} is the linear predictor
(intercept + covariates) and \code{W} follows a distribution that depends
only on \code{dist} (extreme-value for \code{"exponential"}/\code{"weibull"},
standard normal for \code{"lognormal"}, standard logistic for
\code{"loglogistic"}) -- see \code{\link[=ertte_predict]{ertte_predict()}} and \code{.ertte_dist_info()}.
}
\examples{
mod <- ertte_model(survival::Surv(time, event) ~ aucss, ertte_data)
mod

# other AFT distributions are also supported
mod_ln <- ertte_model(survival::Surv(time, event) ~ aucss, ertte_data, dist = "lognormal")
mod_ln

}
