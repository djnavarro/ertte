% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ertte-simulate.R
\name{simulate.ertte_model}
\alias{simulate.ertte_model}
\title{Simulate from an exposure-response TTE model}
\usage{
\method{simulate}{ertte_model}(object, nsim = 100, seed = NULL, newdata = NULL, ...)
}
\arguments{
\item{object}{An ertte model object, as returned by \code{\link[=ertte_model]{ertte_model()}}}

\item{nsim}{Number of simulation replicates}

\item{seed}{Optional seed. If \code{NULL} (the default), one is chosen
automatically and reported via a message (since it determines the
actual simulated values returned).}

\item{newdata}{Data frame to simulate from. Defaults to the data the
model was fitted to. Must contain the original response columns
(\code{time}/\code{event}, as named in the model's \code{Surv()} call) -- see Details.}

\item{...}{Unused, present for compatibility with the \code{simulate()}
generic}
}
\value{
A tibble with one row per observation per replicate:
\code{dat_id}/\code{sim_id}, sampled \verb{coef_*} columns, \code{sim_time} (the
simulated event/censoring time), and \code{sim_event} (1 = event, 0 =
censored).
}
\description{
\code{simulate()} method for \code{ertte_model} objects.
}
\details{
Coefficients are sampled from the asymptotic sampling
distribution implied by \code{vcov(object)}, and event times are drawn by
inverse-CDF sampling from the fitted AFT distribution at each sampled
coefficient vector (see \code{\link[=ertte_model]{ertte_model()}} Details for the
log-location-scale representation used). Simulated event times are
capped at each row's \emph{observed} exit time (\code{sim_time <- pmin(sim_time_raw, observed_time)}, with \code{sim_event} set accordingly)
to reproduce the study's observed censoring/follow-up pattern -- a
documented simplification, since the true administrative censoring
time for subjects who had an event isn't otherwise available (see
\code{.ertte_simulate_draws()}).
}
\examples{
mod <- ertte_model(survival::Surv(time, event) ~ aucss, ertte_data)
sim <- simulate(mod, nsim = 20, seed = 1234)
sim

}
