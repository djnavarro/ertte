% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ertte-scm.R
\name{ertte_term}
\alias{ertte_term}
\alias{ertte_add_term}
\alias{ertte_remove_term}
\title{Add or remove a covariate term from an exposure-response TTE model}
\usage{
ertte_add_term(mod, term, quiet = FALSE)

ertte_remove_term(mod, term, quiet = FALSE)
}
\arguments{
\item{mod}{An ertte model object, as returned by \code{\link[=ertte_model]{ertte_model()}}}

\item{term}{A one-sided formula naming the term to add/remove, e.g.
\code{~ sex}}

\item{quiet}{If \code{TRUE}, suppress the warning issued when the term
can't be added/removed (because it's already in the model / isn't in
the model, respectively)}
}
\value{
An ertte model object. If the term can't be added/removed
(see \code{quiet}), the original \code{mod} is returned unchanged.
}
\description{
Add or remove a single covariate term from an existing ertte model,
returning a new fitted model object.
}
\details{
These functions are not typically called directly; they
underpin \code{\link[=ertte_scm_forward]{ertte_scm_forward()}} and \code{\link[=ertte_scm_backward]{ertte_scm_backward()}}. Named and
shaped to match the companion \code{erglm} package's
\code{erglm_add_term()}/\code{erglm_remove_term()}: covariates enter as plain
additive terms on the AFT location scale (linear for continuous
covariates, factor levels for categorical ones) -- the richer
"continuous covariate as power function" parameterisation described
in the package's design issue is not yet implemented (see AGENTS.md).
}
\examples{
mod <- ertte_model(survival::Surv(time, event) ~ aucss, ertte_data)
mod2 <- ertte_add_term(mod, ~ sex)
mod3 <- ertte_remove_term(mod2, ~ sex)
}
