% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ertte-core.R, R/ertte-coxph.R
\name{ertte_fun}
\alias{ertte_fun}
\alias{ertte_fun.ertte_aft}
\alias{ertte_fun.ertte_coxph}
\title{Prediction function for an exposure-response TTE model}
\usage{
ertte_fun(object, ...)

\method{ertte_fun}{ertte_aft}(object, ...)

\method{ertte_fun}{ertte_coxph}(object, ...)
}
\arguments{
\item{object}{An ertte model, as returned by \code{\link[=ertte_aft]{ertte_aft()}} or
\code{ertte_coxph()} (not yet implemented)}

\item{...}{Passed to methods}
}
\value{
A function with arguments \code{param}, \code{data}, and \code{time}.
\itemize{
\item The \code{param} argument should be a vector of location coefficients;
defaults to \code{coef(object)} (the fitted coefficients) if not supplied.
\item The \code{data} argument should be a data frame or tibble; defaults to
\code{object$data} (the data the model was fitted to) if not supplied.
\item The \code{time} argument gives the time(s) at which to evaluate the
survival function; recycled against \code{data}.
}
}
\description{
Prediction function for an exposure-response TTE model
}
\details{
\code{ertte_fun()} is a generic, with methods for each supported
engine -- see \code{\link[=ertte_fun.ertte_aft]{ertte_fun.ertte_aft()}}. Named \code{ertte_fun()} for
consistency with the companion \code{erglm}/\code{emaxnls} packages'
\code{erglm_fun()}/\code{emax_fun()}, which serve the same purpose for their
respective model classes.

The \code{ertte_aft} method takes a fitted AFT model as input and
returns a function that evaluates the survival function \code{S(t)} at
user-specified parameters, data, and times (e.g. for VPCs or other
counterfactual simulation scenarios). The returned function checks
that \code{param} is numeric and has one entry per column of the model
matrix implied by \code{data}, erroring informatively rather than failing
with a cryptic "non-conformable arguments" error from matrix
multiplication. \code{scale} is always taken from the fitted \code{object}, not
from \code{param} (the coefficient vector from \code{coef()} never includes
it).

The \code{ertte_coxph} method returns a function that evaluates
\verb{S(t | x) = S0(t)^exp((x - xbar)'param)}, where \code{S0(t)} is the fitted
baseline survival curve (via \code{survival::basehaz(object, centered = TRUE)}, held constant beyond the last observed time, matching
\code{ertte_predict.ertte_coxph()}) and \code{xbar} is \code{object$means} (the
covariate means \code{coxph()} centers the partial likelihood on when
fitting -- centering matters here because \code{basehaz()}'s baseline is
defined relative to it, not to \code{x = 0}). As with
\code{ertte_fun.ertte_aft()}, \code{param} only varies the linear predictor:
the baseline hazard is always taken from the fitted \code{object}, never
recomputed for a hypothetical \code{param} (that would need refitting the
partial likelihood's risk sets) -- matching the level of
approximation used elsewhere in this package (e.g. \code{scale} for AFT
models is likewise held fixed). Since Cox models have no intercept,
\code{param} has one entry per covariate with no \code{"(Intercept)"} column,
unlike \code{ertte_fun.ertte_aft()}.
}
\examples{
mod <- ertte_aft(survival::Surv(time, event) ~ aucss, ertte_data)
mod_fun <- ertte_fun(mod)

# no arguments: reproduces the fitted model's own survival predictions
s1 <- mod_fun(time = 60)

# user modifies the parameters
par2 <- coef(mod)
par2["(Intercept)"] <- par2["(Intercept)"] + 1
s2 <- mod_fun(param = par2, time = 60)

mod_cox <- ertte_coxph(survival::Surv(time, event) ~ aucss, ertte_data)
mod_cox_fun <- ertte_fun(mod_cox)

# no arguments: reproduces the fitted model's own survival predictions
s1 <- mod_cox_fun(time = 60)

# user modifies the parameters
par2 <- coef(mod_cox)
par2["aucss"] <- par2["aucss"] * 1.5
s2 <- mod_cox_fun(param = par2, time = 60)

}
