% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ertte-family.R
\name{ertte_select_distribution}
\alias{ertte_select_distribution}
\title{Select an AFT distribution by AIC}
\usage{
ertte_select_distribution(
  formula,
  data,
  candidates = c("exponential", "weibull", "lognormal", "loglogistic")
)
}
\arguments{
\item{formula}{Model formula, as for \code{\link[=ertte_aft]{ertte_aft()}}}

\item{data}{Data set}

\item{candidates}{Character vector of candidate \code{dist} values to try.
Defaults to all four tested/supported distributions.}
}
\value{
A list with two elements: \code{comparison} (a tibble with one row
per candidate: \code{dist}, \code{logLik}, \code{aic}, \code{bic}, \code{converged}, sorted by
AIC) and \code{model} (the best-fitting \code{ertte_aft} model, i.e. the one
with lowest AIC).
}
\description{
Fits each candidate AFT distribution to the same formula/data and
selects the best fit by AIC.
}
\details{
Ties (to floating point) are broken by the order \code{candidates}
is given in, i.e. the first-listed of the tied candidates is
returned as \code{model}.

\code{candidates} must be a non-empty character vector with no missing
values; each element must also separately name one of the
tested/supported distributions (see \code{\link[=ertte_aft]{ertte_aft()}}'s \code{dist}
argument). \code{candidates = character(0)} errors rather than silently
fitting nothing and returning a degenerate \verb{list(comparison = <0-row tibble>, model = NULL)}.
}
\examples{
# (uses ertte_aft() internally for each candidate distribution)
cmp <- ertte_select_distribution(Surv(time, event) ~ aucss, ertte_data)
cmp$comparison
cmp$model

}
