% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ertte-predict.R
\name{ertte_predict}
\alias{ertte_predict}
\alias{ertte_predict.ertte_aft}
\alias{ertte_predict.ertte_coxph}
\title{Survival-probability predictions for exposure-response TTE models}
\usage{
ertte_predict(object, ...)

\method{ertte_predict}{ertte_aft}(object, newdata = NULL, time, conf_level = 0.95, ...)

\method{ertte_predict}{ertte_coxph}(object, newdata = NULL, time, conf_level = 0.95, ...)
}
\arguments{
\item{object}{An ertte model}

\item{...}{Passed to methods}

\item{newdata}{Data frame containing cases to be predicted. Defaults
to the data the model was fitted to.}

\item{time}{Numeric vector of times at which to compute survival
probabilities}

\item{conf_level}{Confidence level for the intervals. Defaults to \code{.95}.}
}
\value{
A tibble with one row per combination of \code{newdata} row and
\code{time}
}
\description{
Computes fitted survival probabilities \code{S(t)} and confidence intervals
from a fitted ertte model, for one or more rows of \code{newdata} at one or
more \code{time} values.
}
\details{
\code{ertte_predict()} is a generic function, with two supplied
methods, one for parametric AFT exposure-response models (i.e.,
\code{ertte_aft} classed objects) and another for Cox proportional hazard
exposure-response models (i.e., objects with class \code{ertte_coxph}).

The \code{ertte_aft} method computes the linear predictor (and
its standard error) via \code{predict(object, newdata, type = "linear", se.fit = TRUE)}, then converts to a survival probability \code{S(t) = 1 - F((log(t) - mu) / scale)}, where \code{F} is the base distribution's CDF
implied by \code{object}'s \code{dist} (see \code{\link[=ertte_aft]{ertte_aft()}} Details). Confidence
intervals are Wald intervals on \code{mu} (a \code{qnorm()} z-score times the
standard error), back-transformed the same way -- parameter
uncertainty in \code{scale} is not propagated, matching the level of
approximation used throughout this package (e.g. \code{erglm_predict()}'s
equivalent in the companion \code{erglm} package). \code{conf_level} must be a
single number between 0 and 1 (inclusive); other values error rather
than silently producing a reversed or \code{NaN} interval.

The \code{ertte_coxph} method delegates to
\code{survival::survfit(object, newdata, conf.int = conf_level)}, which
computes a per-row survival curve \code{S(t | x) = S0(t)^exp(lp(x) - lp(xbar))} from the fitted baseline hazard (Breslow or Efron,
matching \code{object$method}) and the linear predictor, then evaluates it
at \code{time} via \code{summary(..., extend = TRUE)} -- \code{extend = TRUE} allows
\code{time} to exceed the last observed follow-up time, holding survival
constant beyond it (the usual step-function extrapolation) rather
than erroring. Confidence intervals come from \code{survfit()}'s own
\code{conf.type = "log"} transform (Wald on \code{log(-log(S))}), which is
better suited to a probability bounded in \verb{[0, 1]} than the plain
Wald interval \code{ertte_predict.ertte_aft()} uses on the linear
predictor -- the two methods' intervals are not directly comparable
as a result, which is expected given the different model structures.
}
\examples{
# predictions for an AFT model
mod_aft <- ertte_aft(Surv(time, event) ~ aucss, ertte_data)
ertte_predict(mod_aft, ertte_data[1:5, ], time = c(30, 60, 90))

# predictions for a Cox model
mod_cox <- ertte_coxph(Surv(time, event) ~ aucss, ertte_data)
ertte_predict(mod_cox, ertte_data[1:5, ], time = c(30, 60, 90))

}
